#include "main.h"
#include <stdio.h>
/**
 *_abs - prints the absolute value of an integer
 *@int: integer value
*Return: Always 0
 */
int _abs(int)
{
if (int > 0)
return (0);
else
return (int * -1);
}
